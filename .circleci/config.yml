version: 2
jobs:
  build:
    environment:
      BASH_ENV: "~/.nix-profile/etc/profile.d/nix.sh"

    macos:
      xcode: "8.3"
      
    steps: 
      - checkout

      - run: git config --global user.email "travis-ci@example.com"
      - run: git config --global user.name "Travis-CI"

      - run:
          command: echo "Yes" | ./try-pure --command "exit 0"
          no_output_timeout: 3599
